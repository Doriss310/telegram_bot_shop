services:
  bot:
    build: .
    container_name: telegram_shop_bot
    restart: always
    network_mode: host
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
